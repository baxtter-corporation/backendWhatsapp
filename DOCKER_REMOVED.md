The `Docker/` directory has been deprecated and is no longer used in this repository.

Reason: the project now focuses on running services via local tooling or cloud infrastructure and maintaining multiple Docker configurations caused maintenance overhead and build issues in some environments (e.g., BuildKit secret errors).

If you need any Docker assets back, restore from git history or contact the maintainers.
